使用须知：
在操作action、data、object三个包内的文件时请注意：
        action：包内存放的都是基于浏览器的操作，比如：对象的sendkey，click
        data：包内存放的是元素的定位方式、进行sendkey操作是传递的参数
        object：包内存放的是界面元素的定位


逻辑思路：
1、首先获取到excel中存放的用户名和密码并把每一行对应的用户名和密码做成一个列表，最后把所有的用户名和密码整理成一个大的列表
2、根据用户名和密码列表的长度去定位对象并传递值，同时把用户名和密码传递过去，定位到一个对象之后马上对这个对象进行操作并在代码里写入判断条件，如果excel中的
   标签名称和传递的对象名称相同并且操作方式满足判断条件再去sendkey或者click